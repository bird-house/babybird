[buildout]
develop = .

# buildout options
show-picked-versions = true
newest = false
download-cache = downloads
versions = versions

## extensions

# use python site-packages
# https://pypi.python.org/pypi/buildout.locallib/ 
extensions = buildout.locallib

parts = 
    conda
    mybird
    pywps
    ipython
    nose
    docker
      
[settings]
hostname = localhost
http-port = 38101
https-port = 48101
output-port = 38090
log-level = WARN
user = ''

[versions]
zc.buildout= >=2.4.0
buildout.locallib= >=0.3.1
zc.recipe.egg= >=2.0.1
birdhousebuilder.recipe.conda= >=0.2.5
birdhousebuilder.recipe.docker= >=0.3.2
birdhousebuilder.recipe.pywps= >=0.2.6
birdhousebuilder.recipe.nginx= >=0.2.3
birdhousebuilder.recipe.supervisor= >=0.2.5

[conda]
recipe = birdhousebuilder.recipe.conda
pkgs = 
     pywps
     nose
     ipython

[mybird]
recipe = zc.recipe.egg
eggs =
     pywps
interpreter = python

[pywps]
recipe = birdhousebuilder.recipe.pywps
sites = mybird
user = ${settings:user}
keywords = WPS, PyWPS, Birdhouse, Emu, Python, Test
providerName = Birdhouse/Emu
providerSite = http://mybird.readthedocs.org/en/latest/
processesPath = ${buildout:directory}/processes
hostname = ${settings:hostname}
http_port = ${settings:http-port}
https_port = ${settings:https-port}
output_port = ${settings:output-port}
logLevel = ${settings:log-level}

title = MyBird
abstract = My WPS processes.

[ipython]
recipe = zc.recipe.egg
eggs =
    ipython
    ${mybird:eggs}
entry-points =
   ipython=IPython:start_ipython
scripts =
    ipython=ipython

[nose]
recipe = zc.recipe.egg
eggs =
    nose
    ${mybird:eggs}
entry-points =
    nosetests=nose:run_exit
scripts =
    nosetests=nosetests

[docker]
recipe = birdhousebuilder.recipe.docker
image-name = birdhouse/bird-base
image-version = latest
description = MyBird Web Processing Services







 


